FROM golang:1.26-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -trimpath -ldflags="-s -w" -o /speaker ./cmd/speaker

FROM gcr.io/distroless/static:nonroot
COPY --from=builder /speaker /speaker
ENTRYPOINT ["/speaker"]
